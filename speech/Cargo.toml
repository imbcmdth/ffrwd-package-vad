[package]
name = "speech"
version = "0.1.0"
edition = "2021"

[lib]
name = "speech"
crate-type = ["cdylib"]

[dependencies]
vad-core = { path = "../core" }
wit-bindgen = "0.57.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
